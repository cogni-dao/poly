# VCS
.git
.gitignore
.gitattributes

# Node / build artifacts
**/node_modules
**/.next
**/build
**/coverage
**/out
**/dist
**/*.tsbuildinfo

# Env and local state
.env*
.DS_Store
.vscode
.idea
.context

# CI and docs are not needed in app images
.github
docs
*.md
!work/
!work/items/
!work/items/*.md
!work/projects/
!work/projects/*.md

# Keep runtime DB scripts, but omit non-runtime scripts if a fork adds them.
scripts/ci
scripts/bootstrap
